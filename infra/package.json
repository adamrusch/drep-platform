{
  "name": "@drep-platform/infra",
  "version": "0.1.0",
  "private": true,
  "description": "DRep Platform CDK Infrastructure",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint bin lib",
    "synth": "cdk synth --profile drep-platform",
    "diff": "cdk diff --profile drep-platform",
    "clean": "rm -rf cdk.out"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.147.0",
    "constructs": "^10.3.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@types/node": "^20.14.0",
    "aws-cdk": "^2.147.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.4.5"
  }
}
